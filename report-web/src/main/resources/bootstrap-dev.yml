spring:
  cloud:
    config:
      name: jaagro-config-report-8030-client     #需要从github上读取的资源名称，注意没有yml后缀名
      profile: dev
      label: master
      uri: http://tools.jaagro.com:9040     #SpringCloudConfig获取的服务地址

feignClient:
  application:
    crm: crm
    tms: tms
    user: uaa