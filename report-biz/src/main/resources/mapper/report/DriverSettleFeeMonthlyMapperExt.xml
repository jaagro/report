<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jaagro.report.biz.mapper.report.DriverSettleFeeMonthlyMapperExt">
    <select id="selectByCriteria" resultType="com.jaagro.report.api.dto.settlemanage.ReturnSettleDriverFeeMonthlyDto"
            parameterType="com.jaagro.report.api.dto.settlemanage.ListDriverFeeCriteria">
        select
        <include refid="Base_Column_List"/>
        from driver_settle_fee_monthly
        <where>
            <if test="driverName!=null">
                and driver_name like concat('%',#{driverName},'%')
            </if>
            <if test="truckNumber!=null">
                and truck_number like concat('%',#{truckNumber},'%')
            </if>
            <if test="month!=null">
                and report_time=#{month}
            </if>
            order by create_time desc
        </where>
    </select>
    <insert id="batchSettleFeeMonthInsert" parameterType="list">
        insert into driver_settle_fee_monthly (id, driver_id, driver_name, truck_id, truck_number, report_time,
        start_time, end_time,
        total_waybill, total_quantity, total_weight, total_freight, total_anomaly_fee, modify_time)
        values
        <foreach collection="driverSettleFeeMonthlyList" item="item" separator=",">
            (#{item.id}, #{item.driverId}, #{item.driverName},
            #{item.truckId}, #{item.truckNumber},
            #{item.reportTime},#{item.startTime},
            #{item.endTime}, #{item.totalWaybill},
            #{item.totalQuantity}, #{item.totalWeight},
            #{item.totalFreight}, #{item.totalAnomalyFee},
            #{item.modifyTime})
        </foreach>
    </insert>
    <delete id="deleteByReportTime">
        DELETE
        FROM driver_settle_fee_monthly
        WHERE report_time = #{reportTime}
    </delete>
</mapper>