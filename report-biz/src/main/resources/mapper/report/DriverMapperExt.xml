<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jaagro.report.biz.mapper.report.DriverMapperExt">
    <select id="listDriverInfo" resultType="com.jaagro.report.api.dto.settlemanage.ReturnDriverInfoDto">
	select DISTINCT dr.id,dr.name,dr.truck_id,tk.truck_number
	from userserver.driver dr left join crmserver.truck tk on (dr.truck_id=tk.id)
    </select>
    <select id="listWaybillIdByCriteria" resultType="java.lang.Integer">
	      select DISTINCT wb.id from userserver.driver dr  inner join tmsserver2.waybill wb on (dr.id=wb.driver_id)
		  where  wb.driver_id=#{driverId}
    </select>
</mapper>